name: kakao_map_editting
description: kakao map editting packages
version: 0.0.1
homepage: https://github.com/baehoons/kakao_map_flutter_packages

environment:
  sdk: '>=2.18.0-37.0.dev <3.0.0'
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^3.0.1
  location: ^4.3.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
